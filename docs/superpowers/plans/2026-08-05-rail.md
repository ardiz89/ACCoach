# Il rail — piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dare alla reportistica una colonna sinistra persistente — mappa del giro
+ curve ordinate per tempo perso — che ricordi quale curva stai guardando mentre
cambi scheda.

**Architecture:** un solo `<aside id="rail">`, fratello dei dieci `div#view-*`
dentro un palco flex a due colonne. Nessuno stato nuovo: la curva selezionata *è*
`RANGE`, la finestra globale introdotta da PR #78. Nessun endpoint nuovo: il rail
si costruisce da `DATA.corners` e `DATA.losses`, che il payload già porta.

**Tech Stack:** ES5 semplice in `src/accoach/web/app.js` (nessun build step,
nessun framework), CSS a mano in `style.css`, catalogo i18n piatto in `i18n.js`.
Test in pytest che leggono i sorgenti web come testo (`tests/test_web_views.py`).

**Spec:** `docs/superpowers/specs/2026-08-05-rail-design.md`
**Branch:** `feat/rail` (già creato, spec già committato)

## Global Constraints

- **Niente framework, niente build step.** `app.js` è un unico file ES5 servito
  così com'è. Non introdurre `import`/`export`, moduli, o dipendenze.
- **Il rail non zooma mai.** Mostra sempre il giro intero, anche con la finestra
  accesa.
- **Il rail non è mai una colonna vuota.** Su un giro senza coordinate la mappa
  lascia il posto a una riga di testo e la lista curve resta viva.
- **Ogni chiave i18n nuova va dichiarata in `en` E `it`.** Una chiave mancante non
  solleva niente: `t()` restituisce la chiave stessa e la pagina mostra
  `rail.whole` al posto di «Tutto il giro».
- **Commenti in italiano**, come il codice recente di questo file. Spiegano
  *perché*, non *cosa*.
- **Le sei schede col rail** sono esattamente: `flow`, `compare`, `map`, `line`,
  `sectors`, `dynamics`. Le quattro senza: `training`, `session`, `stint`,
  `progress`.
- **Larghezza del rail: 220 px.** Sotto i 1100 px di viewport il rail si nasconde
  (l'app si legge su tablet accanto al volante — documentato in `style.css:296`).
- Test: `python -m pytest tests/test_web_views.py tests/test_web_i18n_keys.py -q`

---

### Task 1: Il palco a due colonne

Struttura e layout, con il rail vuoto. Nessun contenuto ancora: questa task
esiste da sola perché è quella che può rompere il layout di **tutte e dieci** le
schede, e va potuta bocciare senza toccare il resto.

**Files:**
- Modify: `src/accoach/web/index.html` (avvolge i dieci pannelli, righe 56-~420)
- Modify: `src/accoach/web/style.css` (dopo la regola `main`, riga 268)
- Modify: `src/accoach/web/app.js:248-260` (`showView`)
- Test: `tests/test_web_views.py` (in coda)

**Interfaces:**
- Produces: `RAIL_VIEWS` (array di stringhe, le sei viste col rail); la classe
  `body.railed`; gli elementi `#rail`, `#c-rail`, `#rail-nomap`, `#rail-list`.

- [ ] **Step 1: Scrivi i test che falliscono**

In coda a `tests/test_web_views.py`:

```python
# --- il rail ---------------------------------------------------------------

def test_the_rail_lives_outside_every_view_panel():
    """Un rail dentro un pannello sarebbe sei rail: sei canvas, sei hover da
    cablare e sei posti dove ricordarsi di ridisegnare. Fuori da tutti, è uno."""
    where = _view_of_ids()
    assert "rail" in where, "manca #rail in index.html"
    assert where["rail"] == "", "il rail sta dentro un pannello di vista"


def test_every_view_panel_lives_inside_the_stage():
    """Il palco è la griglia a due colonne: un pannello fuori tornerebbe a
    larghezza piena e finirebbe sotto il rail."""
    stage = _HTML.split('<div class="stage">')[1].split('<!-- /stage -->')[0]
    for panel in _PANELS:
        assert f'id="view-{panel}"' in stage, panel


def test_the_rail_is_declared_once_and_only_for_views_that_exist():
    """Un elenco scritto due volte è un elenco che diverge."""
    assert _APPJS.count("const RAIL_VIEWS") == 1
    body = _APPJS.split("const RAIL_VIEWS = [")[1].split("]")[0]
    for name in re.findall(r'"([\w-]+)"', body):
        assert name in _TABS, name


def test_switching_tab_says_whether_this_one_has_a_rail():
    block = _APPJS.split("function showView(")[1].split("\n}")[0]
    assert 'classList.toggle("railed"' in block
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `python -m pytest tests/test_web_views.py -q -k rail`
Expected: FAIL — `manca #rail in index.html`

- [ ] **Step 3: Avvolgi i pannelli nel palco (`index.html`)**

Subito **dopo** `<section id="lapbar" class="lapbar"></section>` (riga 56) e
**prima** del commento che apre `view-flow`, inserisci:

```html
  <!-- Il palco: il rail a sinistra, le dieci viste a destra. Il rail sta qui e
       non dentro i pannelli perché non appartiene a nessuna vista — è il livello
       «curva» dell'architettura a oggetti annidati, e deve sopravvivere al
       cambio di scheda. Un rail per pannello sarebbe sei canvas e sei hover. -->
  <div class="stage">
    <aside id="rail" class="rail">
      <div class="rail-map">
        <canvas id="c-rail"></canvas>
        <!-- I giri registrati prima dello schema con le coordinate non hanno una
             mappa. Il rail non diventa per questo una colonna vuota: il disegno
             lascia il posto a questa riga e la lista curve sotto resta viva. -->
        <div id="rail-nomap" class="rail-nomap hidden" data-i18n="rail.nomap">This lap has no map.</div>
      </div>
      <nav id="rail-list" class="rail-list"></nav>
    </aside>
    <div class="views">
```

Poi, **dopo** l'ultimo `</div>` che chiude `view-progress` (l'ultimo pannello) e
prima di qualsiasi `<script>`, chiudi:

```html
    </div><!-- /views -->
  </div><!-- /stage -->
```

Verifica di aver chiuso nel punto giusto: fra `<div class="views">` e
`</div><!-- /views -->` devono stare tutti e dieci i `<div id="view-…">` e niente
altro (non `#lapbar`, che resta sopra il palco).

- [ ] **Step 4: Il layout (`style.css`)**

Subito dopo la regola `main { … }` (riga 268):

```css
/* Il palco a due colonne. Il gutter — che cresce con la finestra invece che con
   un wrapper, vedi il commento a --page — si sposta QUI, e dentro `.views` viene
   azzerato: le venticinque bande che scrivono `padding: … var(--gut)` ereditano
   la variabile e si allineano da sole, senza toccarne nemmeno una. Senza questo
   azzeramento, su un 2560 il gutter si applicherebbe due volte e fra il rail e i
   grafici resterebbero 480 px di vuoto. */
.stage { display: flex; align-items: flex-start; gap: 16px; padding: 0 var(--gut); }
.stage > .views { flex: 1 1 auto; min-width: 0; --gut: 0px; }

/* Sticky: scorrendo un debrief lungo la mappa resta dov'è. `top` sotto le
   schede, che non sono sticky, quindi basta un filo d'aria. */
.rail {
  flex: 0 0 220px; position: sticky; top: 8px;
  padding: 16px 0 24px; align-self: flex-start;
}
/* Sulle quattro schede che stanno SOPRA il giro (Sessione, Passo gara,
   Andamento, Allenamento) l'oggetto è un altro e una curva non significa
   niente: meglio niente colonna che una colonna che non risponde. */
body:not(.railed) .rail { display: none; }

.rail-map { position: relative; }
.rail-map canvas { width: 100%; height: 150px; cursor: crosshair; display: block; }
.rail-nomap { font-size: 11px; color: var(--muted); line-height: 1.5; padding: 8px 0; }

/* Il tablet accanto al volante: sotto i 1100 px la colonna mangerebbe un quinto
   della larghezza. Il rail sparisce e i chip della Traiettoria restano il modo
   di scegliere una curva. */
@media (max-width: 1100px) {
  .rail { display: none; }
  .stage { display: block; }
  .stage > .views { --gut: max(24px, calc((100% - var(--page)) / 2)); }
  .stage { padding: 0; }
}
```

- [ ] **Step 5: La classe `railed` (`app.js`)**

Sopra `function showView(name)` (riga ~246):

```js
// Le sei schede che parlano di UN GIRO, e quindi di una curva. Le altre quattro
// stanno sopra il giro nella gerarchia (una sessione, uno stint, lo storico, un
// piano): lì il rail mostrerebbe un giro che non c'entra con quello che leggi, e
// cliccare una curva non cambierebbe niente. Un comando che non risponde è
// peggio di un comando assente.
const RAIL_VIEWS = ["flow", "compare", "map", "line", "sectors", "dynamics"];
```

Dentro `showView`, **prima** della chiamata a `redrawCurrentView()` (così il
rail conosce già la sua larghezza quando disegna):

```js
  document.body.classList.toggle("railed", RAIL_VIEWS.indexOf(name) >= 0);
```

- [ ] **Step 6: Esegui i test**

Run: `python -m pytest tests/test_web_views.py tests/test_web_i18n_keys.py -q`
Expected: PASS (i quattro nuovi inclusi)

- [ ] **Step 7: Guarda tutte e dieci le schede**

Avvia: `python -m accoach.cli report --demo` (o il comando con cui il pilota apre
la reportistica) e apri la pagina nel browser. Su **ognuna** delle dieci schede
controlla: nessuna barra di scorrimento orizzontale, il contenuto allineato con
l'intestazione sopra, le quattro schede senza rail a larghezza piena. Poi
restringi la finestra sotto i 1100 px e verifica che il rail sparisca senza
lasciare un buco.

Questo è il passo che giustifica l'esistenza della task: i test statici non
vedono un layout storto.

- [ ] **Step 8: Commit**

```bash
git add src/accoach/web/index.html src/accoach/web/style.css src/accoach/web/app.js tests/test_web_views.py
git commit -m "Il palco a due colonne: il rail ha un posto, ancora vuoto"
```

---

### Task 2: La mappa del rail, disegnata fuori dallo switch

**Files:**
- Modify: `src/accoach/web/app.js` (nuova `drawRail` accanto a `drawMiniMap`,
  riga ~1021; `redrawCurrentView` riga 202-218; `loadCombo` riga ~1257)
- Modify: `src/accoach/web/i18n.js` (chiave `rail.nomap`)
- Test: `tests/test_web_views.py`

**Interfaces:**
- Consumes: `RAIL_VIEWS`, `#c-rail`, `#rail-nomap` (Task 1); `drawMapTo(canvas,
  missing, a, cx, mode) → {rv, X, Y} | null` (esistente, `app.js:1027`).
- Produces: `drawRail()`; `RAIL_HIT` (`{rv, X, Y} | null`, il trasformo schermo
  che il Task 4 usa per l'hover).

- [ ] **Step 1: Scrivi i test che falliscono**

In coda a `tests/test_web_views.py`:

```python
def test_the_rail_is_drawn_outside_the_per_view_switch():
    """`redrawCurrentView` è uno switch per-vista e il rail non appartiene a
    nessuna vista. Dentro un ramo, al cambio scheda resterebbe fermo all'ultimo
    giro e dopo un resize l'hover punterebbe al posto sbagliato.

    Il criterio è l'indentazione: una riga a sé, ai due spazi del corpo della
    funzione, non può stare dentro un `if` — un ramo la indenterebbe di più o se
    la porterebbe sulla propria riga."""
    block = _APPJS.split("function redrawCurrentView()")[1].split("\n}")[0]
    assert re.search(r"^  drawRail\(\);$", block, re.M), \
        "drawRail() dev'essere una riga a sé, fuori dallo switch per-vista"


def test_a_lap_without_coordinates_still_has_a_rail():
    """I giri ACC registrati prima dello schema con le coordinate sono esattamente
    questo caso, e col rail persistente diventerebbero una colonna vuota su tutte
    le schede."""
    assert 'id="rail-nomap"' in _HTML
    block = _APPJS.split("function drawRail()")[1].split("\n}")[0]
    assert '$("rail-nomap")' in block, "il segnaposto non viene passato a drawMapTo"


def test_the_rail_is_drawn_when_a_lap_loads():
    block = _APPJS.split("async function loadCombo")[1].split("\n}")[0]
    assert "drawRail();" in block
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `python -m pytest tests/test_web_views.py -q -k rail`
Expected: FAIL — `drawRail();` non compare in `redrawCurrentView`

- [ ] **Step 3: Scrivi `drawRail` (`app.js`, dopo `drawMiniMap`, riga ~1021)**

```js
// La mappa del rail. Sempre a GIRO INTERO, anche con la finestra accesa: il rail
// è il «dove sono», e uno zoom lo trasformerebbe in una seconda copia del
// grafico che stai già guardando. La finestra si legge come un tratto acceso.
let RAIL_HIT = null;   // {rv, X, Y}, il trasformo schermo, per l'hover

function drawRail() {
  // Su una scheda senza rail il canvas ha larghezza zero e `setup` disegnerebbe
  // su una tela di 0 px: lavoro sprecato, e un hit test tarato su niente.
  if (!DATA || !document.body.classList.contains("railed")) return;
  RAIL_HIT = drawMapTo($("c-rail"), $("rail-nomap"), DATA, LAST_HOVER);
  if (RAIL_HIT && RANGE) railWindow(RAIL_HIT);
}

// Il tratto scelto, acceso sopra la mappa già disegnata. Ridisegnare con lo
// stesso contesto è legittimo: `setup` lascia sul canvas la trasformazione del
// device pixel ratio, e `X`/`Y` restituiti da `drawMapTo` sono in pixel CSS —
// gli stessi in cui si ragiona qui.
function railWindow(hit) {
  const ctx = $("c-rail").getContext("2d");
  const rv = hit.rv;
  ctx.save();
  ctx.strokeStyle = "#22D3CE"; ctx.lineWidth = 4;
  ctx.lineCap = "round"; ctx.lineJoin = "round";
  ctx.beginPath();
  let started = false;
  for (let i = 0; i < rv.x.length; i++) {
    const p = rv.pos[i];
    if (p < RANGE.from || p > RANGE.to) { started = false; continue; }
    const px = hit.X(rv.x[i]), py = hit.Y(rv.z[i]);
    started ? ctx.lineTo(px, py) : ctx.moveTo(px, py);
    started = true;
  }
  ctx.stroke();
  ctx.restore();
}
```

- [ ] **Step 4: Chiamala fuori dallo switch (`app.js:202-218`)**

In coda a `redrawCurrentView`, **dopo** l'ultimo `else if`, alla stessa
indentazione dell'`if` iniziale:

```js
  // Il rail non appartiene a nessuna vista, quindi non sta in nessun ramo. Fuori
  // dallo switch è l'unico posto dove il cambio scheda e il resize lo trovano
  // entrambi: `redrawCurrentView` è ciò che il resize chiama (debounced, riga
  // ~4144) e ciò che `showView` chiama in coda.
  drawRail();
```

E in `loadCombo`, subito dopo `redraw(null);` (riga ~1257):

```js
  drawRail();
```

- [ ] **Step 5: Dichiara la chiave i18n (`i18n.js`)**

Accanto alle altre chiavi di vista (l'ordine nel catalogo è per famiglia; mettila
vicino a `"map.missing"`):

```js
    "rail.nomap":      { en: `This lap has no map (recorded before the coordinates arrived). The corners below still work.`,
                         it: `Questo giro non ha mappa (registrato prima delle coordinate). Le curve qui sotto funzionano lo stesso.` },
```

- [ ] **Step 6: Esegui i test**

Run: `python -m pytest tests/test_web_views.py tests/test_web_i18n_keys.py -q`
Expected: PASS

- [ ] **Step 7: Guardalo, incluso un giro senza mappa**

Sulle sei schede col rail la mappa deve comparire e restare identica al cambio
scheda. Poi seleziona un giro **senza coordinate** (i giri ACC di giugno
nell'archivio; in demo, se non ce ne sono, forza il caso mettendo
temporaneamente `if (false)` al posto di `if (!a.has_map)` in `drawMapTo` —
e **rimettilo a posto**): il canvas deve sparire, la riga di testo comparire, e
la colonna non deve restare vuota.

- [ ] **Step 8: Commit**

```bash
git add src/accoach/web/app.js src/accoach/web/i18n.js tests/test_web_views.py
git commit -m "La mappa del rail, fuori dallo switch per-vista"
```

---

### Task 3: Le curve, ordinate per quanto costano

**Files:**
- Modify: `src/accoach/web/app.js` (`drawRailList` accanto a `drawRail`;
  `cornerWindow` riga 2355; le due chiamate di Task 2)
- Modify: `src/accoach/web/style.css` (dopo le regole `.rail-*` di Task 1)
- Modify: `src/accoach/web/i18n.js`
- Test: `tests/test_web_views.py`, `tests/test_api.py`

**Interfaces:**
- Consumes: `#rail-list` (Task 1); `drawRail()` (Task 2); `setRange(r)`
  (`app.js:2362`), `cornerWindow(c)` (`app.js:2355`), `lossColor(sev)`
  (`app.js:1477`).
- Produces: `railRows(a) → {hot: [loss], cold: [corner]}`; `drawRailList()`;
  il campo `RANGE.corner` (indice della curva, o assente).

- [ ] **Step 1: Scrivi il test dell'invariante sul payload (`tests/test_api.py`)**

Il rail aggancia le perdite alle curve **per numero**. Se un giorno una perdita
citasse una curva che non esiste, la riga diventerebbe non cliccabile in
silenzio. In coda a `tests/test_api.py`:

```python
def test_every_loss_points_at_a_corner_that_exists(tmp_path):
    """Il rail aggancia la perdita alla curva per numero (mai per nome: due curve
    possono chiamarsi uguale, e su una pista senza nomi curati si chiamano tutte
    `Corner N`). Un indice che non esiste sarebbe una riga che non risponde."""
    c = _client(tmp_path)
    data = c.get("/api/analysis", params={"car": CAR, "track": TRACK}).json()
    known = {cc["index"] for cc in data["corners"]}
    assert data["losses"], "il giro sintetico ha una curva lenta apposta"
    for loss in data["losses"]:
        assert loss["index"] in known, loss
```

`_client`, `CAR` e `TRACK` sono già in cima al file (righe 9-25): il giro
sintetico è costruito con `slow_corner=0`, quindi una perdita c'è sempre.

- [ ] **Step 2: Scrivi i test strutturali (`tests/test_web_views.py`)**

```python
def test_the_rail_lists_the_clean_corners_too():
    """La classifica dice da dove cominciare; il selettore deve poter aprire
    anche una curva dove non hai perso niente — è lì che si va a vedere cosa hai
    fatto giusto."""
    block = _APPJS.split("function railRows(")[1].split("\n}")[0]
    assert "cold" in block and "hot" in block


def test_the_selected_corner_is_remembered_by_number_not_by_name():
    """Due curve possono chiamarsi uguale: su ogni pista senza nomi curati si
    chiamano tutte `Corner N`, e la riga accesa sarebbe la prima delle due."""
    block = _APPJS.split("function cornerWindow(")[1].split("\n}")[0]
    assert "corner: c.index" in block


def test_clicking_a_corner_moves_the_shared_window():
    block = _APPJS.split("function drawRailList()")[1].split("\n\nfunction")[0]
    assert "setRange(cornerWindow(" in block
    assert "setRange(null)" in block, "manca «Tutto il giro»"
```

- [ ] **Step 3: Esegui i test e verifica che falliscano**

Run: `python -m pytest tests/test_web_views.py tests/test_api.py -q -k "rail or corner_that_exists"`
Expected: FAIL — `railRows` non esiste

- [ ] **Step 4: L'indice nella finestra (`app.js:2355`)**

In `cornerWindow`, aggiungi il campo al valore di ritorno:

```js
  return { from: Math.max(0, c.entry - pad), to: Math.min(1, c.exit + pad),
           source: "corner", label: c.name || ("T" + (c.index + 1)),
           // Il numero, non il nome: il rail deve accendere LA riga giusta, e su
           // una pista senza nomi curati le curve si chiamano tutte «Corner N».
           corner: c.index };
```

- [ ] **Step 5: Le righe (`app.js`, dopo `railWindow`)**

```js
// Le curve del rail in due gruppi. Sopra la classifica — chi è costato di più,
// per primo: è il waterfall promosso a navigazione, l'unica cosa in tutta l'app
// che mostri la DISTRIBUZIONE della perdita invece di una conclusione. Sotto, in
// ordine di pista e senza barretta, le curve dove non hai perso niente: un
// selettore che non le elenca non è un selettore, è una classifica.
function railRows(a) {
  const byIndex = new Map();
  for (const l of (a.losses || [])) if (l.lost_s > 0) byIndex.set(l.index, l);
  const hot = Array.from(byIndex.values()).sort((x, y) => y.lost_s - x.lost_s);
  const cold = (a.corners || []).filter((c) => !byIndex.has(c.index));
  return { hot: hot, cold: cold };
}

function drawRailList() {
  const el = $("rail-list");
  if (!el || !DATA) return;
  const corners = DATA.corners || [];
  const at = (i) => corners.filter((c) => c.index === i)[0];
  const rows = railRows(DATA);
  let mx = 0.05;
  for (const l of rows.hot) mx = Math.max(mx, l.lost_s);
  const sel = RANGE && RANGE.corner != null ? RANGE.corner : null;

  // «Tutto il giro» è il primo elemento e non uno stato assente: finché non è
  // scritto, tornare indietro dalla curva sembra impossibile.
  let html = `<button type="button" class="rail-row whole${RANGE ? "" : " on"}" data-whole="1">` +
             `${t("rail.whole")}</button>`;
  for (const l of rows.hot) {
    const w = (Math.min(l.lost_s / mx, 1) * 100).toFixed(0);
    const sev = Math.min(1, l.lost_s / Math.max(mx, 0.3));
    html += `<button type="button" class="rail-row${sel === l.index ? " on" : ""}" ` +
            `data-i="${l.index}" title="${escAttr(l.message || "")}">` +
            `<span class="n">T${l.index + 1}</span>` +
            `<span class="nm">${l.label}</span>` +
            `<span class="bar"><span class="fill" style="width:${w}%;` +
            `background:${lossColor(sev)}"></span></span>` +
            `<span class="s">−${l.lost_s.toFixed(2)}</span></button>`;
  }
  if (rows.cold.length && rows.hot.length) {
    html += `<div class="rail-sep">${t("rail.clean")}</div>`;
  }
  for (const c of rows.cold) {
    html += `<button type="button" class="rail-row cold${sel === c.index ? " on" : ""}" ` +
            `data-i="${c.index}"><span class="n">T${c.index + 1}</span>` +
            `<span class="nm">${c.name}</span></button>`;
  }
  el.innerHTML = html;

  for (const b of el.querySelectorAll(".rail-row[data-i]")) {
    b.onclick = () => {
      const c = at(parseInt(b.dataset.i, 10));
      if (!c) return;
      setRange(cornerWindow(c));
      redrawCurrentView();
    };
  }
  const whole = el.querySelector(".rail-row[data-whole]");
  if (whole) whole.onclick = () => { setRange(null); redrawCurrentView(); };
}
```

Verifica che `escAttr` esista già in `app.js` (è usata da `drawWaterfall`,
riga 1498). Se il nome fosse diverso, usa quello reale.

- [ ] **Step 6: Chiamala accanto a `drawRail` (`app.js`)**

In `redrawCurrentView`, subito sotto `drawRail();`:

```js
  drawRailList();
```

E in `loadCombo`, subito sotto il `drawRail();` del Task 2:

```js
  drawRailList();
```

- [ ] **Step 7: Le righe, in CSS (`style.css`, dopo `.rail-nomap`)**

```css
.rail-list { display: flex; flex-direction: column; gap: 2px; margin-top: 10px; }
.rail-row {
  display: grid; grid-template-columns: 26px 1fr 34px auto;
  align-items: center; gap: 6px;
  background: transparent; border: 0; border-radius: 8px;
  padding: 5px 6px; text-align: left; cursor: pointer;
  color: var(--muted); font-family: var(--font-ui); font-size: 12px;
}
.rail-row:hover { background: var(--panel); color: var(--text); }
.rail-row.on { background: var(--inset); color: var(--text); }
.rail-row.whole { grid-template-columns: 1fr; font-weight: 600; margin-bottom: 4px; }
.rail-row.cold { grid-template-columns: 26px 1fr; }
.rail-row .n { font-family: var(--font-mono); font-size: 11px; opacity: .7; }
.rail-row .nm { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.rail-row .bar { height: 6px; background: var(--inset); border-radius: 999px; overflow: hidden; }
.rail-row .fill { display: block; height: 100%; border-radius: 999px; }
.rail-row .s { font-family: var(--font-mono); font-size: 11px; }
.rail-sep {
  font-size: 10px; text-transform: uppercase; letter-spacing: .06em;
  color: var(--muted); opacity: .7; padding: 10px 6px 4px;
  border-top: 1px solid var(--line); margin-top: 6px;
}
```

- [ ] **Step 8: Le due chiavi i18n (`i18n.js`)**

Accanto a `"rail.nomap"`:

```js
    "rail.whole":      { en: `Whole lap`, it: `Tutto il giro` },
    "rail.clean":      { en: `nothing lost here`, it: `qui non hai perso niente` },
```

- [ ] **Step 9: Esegui i test**

Run: `python -m pytest tests/test_web_views.py tests/test_web_i18n_keys.py tests/test_api.py -q`
Expected: PASS

- [ ] **Step 10: Guardalo**

Clicca una curva nel rail: i grafici della scheda su cui sei devono ritagliarsi
su quella curva (è la finestra di PR #78), la riga deve restare accesa cambiando
scheda, e la pastiglia col nome della curva deve comparire nella barra readout.
Poi «Tutto il giro» e verifica che tutto torni a giro intero. Controlla una
pista **senza nomi curati**, dove le curve si chiamano tutte `Corner N`: deve
accendersi una riga sola.

- [ ] **Step 11: Commit**

```bash
git add src/accoach/web/app.js src/accoach/web/style.css src/accoach/web/i18n.js tests/
git commit -m "Le curve nel rail: la classifica sopra, il resto del giro sotto"
```

---

### Task 4: L'hover, e la minimappa di Confronto che se ne va

**Files:**
- Modify: `src/accoach/web/app.js` (`wireHover` righe 4002-4135; `redraw`
  riga 2468-2476; `drawMiniMap` righe 1012-1021; `MINI_HIT` riga 24)
- Modify: `src/accoach/web/index.html` (righe 194-199, il blocco `.hero`)
- Modify: `src/accoach/web/style.css` (righe 321-322, 993-997)
- Test: `tests/test_web_views.py`

**Interfaces:**
- Consumes: `RAIL_HIT` (Task 2), `nearestPos(hit, canvas, e)` (locale dentro
  `wireHover`, `app.js:4021`).
- Produces: `hoverTo(p)` — instrada il mirino alla vista attiva; niente dove non
  c'è un consumatore.

- [ ] **Step 1: Scrivi i test che falliscono**

```python
def test_the_rail_hover_is_routed_per_view_not_wired_to_one():
    """L'hover della minimappa chiamava `redraw`, che è la funzione della sola
    vista Confronto. Su un rail che vive su sei schede sarebbe muto sulle altre
    cinque — o peggio, ridisegnerebbe una vista che non è sullo schermo."""
    assert "function hoverTo(" in _APPJS
    block = _APPJS.split("function hoverTo(")[1].split("\n}\n")[0]
    for view in ("map", "dynamics", "line", "compare"):
        assert f'"{view}"' in block, view


def test_the_compare_minimap_is_gone_now_that_the_rail_is_the_map():
    """La stessa figura due volte a venti centimetri: è la ripetizione che il
    panel ha misurato come causa della «mancanza di logica» percepita."""
    assert "c-minimap" not in _HTML
    assert "drawMiniMap" not in _APPJS
    assert "MINI_HIT" not in _APPJS
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `python -m pytest tests/test_web_views.py -q -k "hover or minimap"`
Expected: FAIL — `function hoverTo(` non esiste

- [ ] **Step 3: Scrivi `hoverTo` (`app.js`, sopra `wireHover`, riga ~4001)**

```js
// Dove va a finire un mirino mosso dal rail. Il rail vive su sei schede e ognuna
// ha un modo diverso di mostrare «sei qui»; senza questo instradamento servirebbe
// un `if` per scheda dentro l'handler, ed è così che l'hover della minimappa era
// finito cablato alla sola vista Confronto.
//
// Le schede senza un consumatore di mirino (il flusso guidato, i settori) non
// fanno NIENTE, di proposito: il rail muove il proprio marcatore e basta. Meglio
// un gesto che non risponde di un gesto che ridisegna una vista che non è a
// schermo.
function hoverTo(p) {
  if (!DATA) return;
  LAST_HOVER = p;
  if (VIEW === "compare") redraw(p);
  else if (VIEW === "dynamics") drawDynamics(p);
  else if (VIEW === "line") { if (LINE) renderLine(p); }
  else if (VIEW === "map") {
    drawMap(DATA, p);
    $("map-readout").innerHTML = p == null ? MAP_READOUT_DEFAULT() : readoutHTML(DATA, p);
  }
  drawRail();
}
```

Nota: `redraw(p)` e `drawDynamics(p)` scrivono a loro volta `LAST_HOVER` tramite
il readout. Assegnarlo prima serve alle schede che non lo fanno, così il
marcatore del rail sa dov'è.

- [ ] **Step 4: Cabla il rail e togli la minimappa (`app.js`, in `wireHover`)**

Sostituisci il blocco della minimappa (righe 4050-4060) con:

```js
  // Il rail: stesso gesto della vecchia minimappa di Confronto, ma su sei schede
  // — quindi instradato invece che cablato a una.
  const rail = $("c-rail");
  if (rail) {
    rail.addEventListener("mousemove", (e) => {
      if (!DATA) return;
      const p = nearestPos(RAIL_HIT, rail, e);
      if (p != null) hoverTo(p);
    });
    rail.addEventListener("mouseleave", () => hoverTo(null));
  }
```

Poi togli, nell'ordine:
- la riga `drawMiniMap(DATA, cx);` da `redraw` (riga 2474);
- l'intera funzione `drawMiniMap` col suo commento (righe 1012-1021);
- la dichiarazione `let MINI_HIT = null;` e il suo commento (riga 24).

- [ ] **Step 5: Togli la minimappa dal DOM (`index.html`, righe 194-199)**

Il blocco `.hero` resta, con il solo riepilogo:

```html
    <div class="hero">
      <section id="summary" class="summary"></section>
    </div>
```

E in `style.css` togli `.minimap { … }` con la sua regola canvas (righe 321-326
circa) e le due regole `.hero .minimap` (righe 993 e 997). Se `.hero` a quel
punto contiene un solo figlio, controlla a schermo che il riepilogo non si
stiri: se lo fa, aggiungi `max-width` alla regola `.hero .summary` esistente
invece di riscrivere `.hero`.

- [ ] **Step 6: Esegui i test**

Run: `python -m pytest tests/ -q`
Expected: PASS — l'intera suite, perché questa task tocca funzioni condivise

- [ ] **Step 7: Guardalo su tutte e sei le schede**

Passa il mouse sulla mappa del rail e verifica, scheda per scheda: su Confronto i
mirini dei quattro grafici si muovono e il readout si aggiorna; su Dinamica le
tracce rispondono; su Traiettoria il disegno della curva risponde; su Mappa la
mappa grande e il suo readout rispondono; su Il giro spiegato e Settori **non
deve succedere niente** tranne il marcatore sul rail. Poi verifica il verso
opposto: muovendo il mouse sui grafici di Confronto, il marcatore sul rail si
muove.

- [ ] **Step 8: Commit**

```bash
git add src/accoach/web/app.js src/accoach/web/index.html src/accoach/web/style.css tests/test_web_views.py
git commit -m "Un hover solo, instradato per vista; la minimappa trasloca nel rail"
```

---

### Task 5: La verifica a schermo, e la PR

Nessun codice nuovo se non escono difetti. In questo progetto è il browser ad
aver trovato tutti i difetti veri della reportistica, e questa è la task dove si
guarda davvero.

**Files:**
- Modify: quello che i difetti richiedono
- Test: `tests/` (un test nuovo per ogni difetto trovato che sia visibile dal
  sorgente)

- [ ] **Step 1: La suite intera**

Run: `python -m pytest tests/ -q`
Expected: PASS, e annota il conteggio.

- [ ] **Step 2: Il giro delle dieci schede**

Con il browser, su ognuna delle dieci: nessuno scorrimento orizzontale, il rail
presente esattamente sulle sei previste, il contenuto allineato. Ripeti a
finestra stretta (sotto 1100 px) e a finestra molto larga (2560 px se
disponibile, dove il doppio gutter sarebbe visibile).

- [ ] **Step 3: I due casi limite**

1. un giro **senza coordinate**: mappa assente, riga di testo presente, lista
   curve viva e cliccabile;
2. una pista **senza nomi curati** (curve `Corner N`): cliccando la terza curva
   si accende la terza riga, non la prima.

- [ ] **Step 4: Il cambio lingua**

Passa a inglese e torna: le tre chiavi nuove (`rail.whole`, `rail.nomap`,
`rail.clean`) devono cambiare lingua senza ricaricare, e la curva selezionata
deve sopravvivere al cambio.

- [ ] **Step 5: Correggi quello che hai visto, con un test dove è possibile**

Un difetto visibile dal sorgente prende un test in `tests/test_web_views.py`, con
lo stesso stile degli altri: il docstring dice **perché** il difetto era
silenzioso.

- [ ] **Step 6: Apri la PR**

```bash
git push -u origin feat/rail
gh pr create --title "Il rail: la colonna che ricorda quale curva stavi guardando" --body "…"
```

Il corpo della PR deve dire, oltre a cosa fa: che il layout è cambiato sotto a
tutte e dieci le schede (non solo alle sei col rail), che la minimappa di
Confronto non è stata cancellata ma **spostata**, e che la sezione `#waterfall`
resta dov'è di proposito — si toglie quando il rail avrà dimostrato di
funzionare.

---

## Fuori perimetro

Confermato dallo spec, da non fare in questo piano: cancellare `#waterfall`,
fondere schede, l'interruttore «Avanzato», la gerarchia dei titoli, la demo che
si autosabota, la sovrapposizione multi-giro, il ripiego al secondo miglior giro.
